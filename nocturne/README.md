# NOCTURNE

一个受电影化实时着色器展示方式启发的原创 WebGL 实验。项目使用原生 HTML、CSS、JavaScript 与 GLSL，无构建步骤、无运行时依赖。

## 本地预览

```bash
python -m http.server 8080
```

然后访问 `http://localhost:8080`。

## GitHub Pages

仓库已包含 GitHub Pages 工作流。推送到 `main` 后，在仓库 Settings → Pages 中将 Source 设为 **GitHub Actions** 即可发布。
